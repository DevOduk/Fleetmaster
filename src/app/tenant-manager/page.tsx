"use client"

export default function HomePage() {

    return (
        <div>
            Tenant management dashboard page. fleetmaster admin must be logged in to see this page
        </div>
    );
}